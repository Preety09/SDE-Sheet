#include<bits/stdc++.h>
int removeDuplicates(vector<int> &arr, int n) { 
    //unordered_map<int,int>m;
    int count=1;
    for(int i=0;i<n-1;i++)
    { 
        if(arr[i]!=arr[i+1])
            count++;
    }
    return count;
    
	// Write your code here.
}
