vector<int> kthSmallLarge(vector<int> &arr, int n, int k)
{ 
    sort(arr.begin(),arr.end());
    int i=k-1;
    int j=n-k;
    vector<int>v;
    v.push_back(arr[i]);
    v.push_back(arr[j]);
    return v;
	// Write your code here.
}
